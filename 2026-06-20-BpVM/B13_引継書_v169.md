# B13 引継書 v169

**前版**: v168（文書のみ・code 未変更）
**日付**: 2026-06-27
**code**: 変更なし。**v167 zip 継続使用**（canonical `engine_reversal_eval.py` sha256 = `5d02397953339e7d2d0cf60fb6d4c99d85b8579e7af62c9c09f611ef3ef9feec` を本日再確認・一致 ✓）。本版は文書のみ。

---

## 0. 今日の到達点（一行）

実観測データで初めて `run_bundle` を回し、**全 BLIND の honest ABSTAIN を出した**。prior は的中。harness は「無いものを無いと言う」段階に入った。

---

## 1. reversal_lane K=3 real_d — 実行・結果

### 1.1 実行条件
- target: `20260625073000`（岩手県沖 M6.9、40.2°N 142.3°E、深さ50km）
- bundle: `[MYG001_NS, IWTH03_ch4, IWT003_NS]`
- 前処理: 各 demean のみ（filter / taper / window 切り出しなし）
- engine: a_prep 既定（n_surr=200, seed=11）
- engine sha256: `5d0239795333…` canonical 確認済

### 1.2 数字（生）

| station | col | len | peak\|demean\| | verdict | s | real_d | gap99 |
|---|---|---|---|---|---|---|---|
| MYG001_NS  | 2 | 16400 |  54.59 gal | BLIND | 4 | 0.0000 | +0.0000 |
| IWTH03_ch4 | 5 | 22100 | 246.38 gal | BLIND | 2 | 0.0000 | +0.0000 |
| IWT003_NS  | 2 | 18500 |  94.02 gal | BLIND | 6 | 0.0000 | +0.0000 |

reason（全本）: `real_d=0.000<0.9 (real-collapse)`
**BUNDLE = `BUNDLE_ABSTAIN`**（diag: 全本 BLIND ⇒ headline は ABSTAIN だが意味は『反転なし』）

### 1.3 読み（director 確定）
- 20260625073000 の K=3 bundle では **reversal_lane は作動せず**。
- `s = 4 / 2 / 6` は **支配 bin の表示に留まる**。反転署名として読まない。
- prior が予想した `ABSTAIN / 全 BLIND（real-collapse）` の通りの結果。
- harness は「FORWARD を急がず、無いものを無いと出した」。これを **成功として記録する**。

---

## 2. 三層エピステミック整理

### 2.1 確定（言える）
- 同一の凍結 instrument（canonical engine sha256 `5d0239795333…`）で初の実観測 real_d を実行できた。
- 出力: K=3 / 3点 BLIND / `real_d=0.0000` / `gap99=+0.0000` / 検出 s=4,2,6（割れ）/ BUNDLE_ABSTAIN。
- これは a_prep の文書化 prior（地震コーダ典型 SNR では ABSTAIN / 全 BLIND が既定）と一致。

### 2.2 観測（条件付き）
- s 検出列は 3点で割れた（4 / 2 / 6）。real_d=0 のため反転署名としては読まない、が、**支配 bin の値**として記録は残す（将来の同地点・他イベント比較のため）。
- IWTH03 同位置 K-NET の **IWT019**（70km・地表）は本 bundle に未投入。同地点検証用に保留中（別枠）。

### 2.3 言えないこと（フェンス）
- 「岩手県沖 M6.9 には B13 反転署名が無い」とは言えない（K=3 / SNR帯 / 帯域非分離 / 単一イベント）。
- 「他イベントで FORWARD が出る／出ない」は本結果から推定できない。
- 「reversal_lane は地震に使えない／使える」も本結果一発では決まらない（director 反復は別ノードでの判断）。
- B13 は instrument。地震予測はしない。

---

## 3. 「全 BLIND を成功として記録する」段階の意味

v154–v167 でハーネスを組んだ理由は、**「無いものを有る風に語らない」装置を作る**ためだった。
v159 で a_prep 経路4（不変 decoy → 偽 FORWARD）を自分で見つけて prior に書き込み、v167 で sha256 で engine を凍結し、v168 で K=3 admissibility を取り、v169 で実観測に当てた——
**結果: 反転無し。harness はそれを `BLIND/ABSTAIN` として正しく吐いた。**
これは harness の機械的成功であり、B13 が地震で何かを「見つけた」記録ではない。

---

## 4. 次の選択肢（director 手番）

並行可能・優先は director 判断:

1. **その5 ドラフトの Zenn 入稿**（v168 §1.6 で査読反映済・図 URL 差替えだけ残）。
2. **記事1/2 系（2005・2022 含む）の統一パイプライン化**——全系列を単一土台に乗せる作業（手順1 の延長）。
3. **本結果の Zenn 記事化**——「全 BLIND を成功として書く」短い実証記事（reversal_lane の現状報告）。
4. **同地点比較実験**——IWT019（K-NET 地表）vs IWTH03（KiK-net 地表 ch4 / 地中 ch1）。同地点 SNR 差の効果検証。bundle 独立性に影響するため本 K=3 とは分離。
5. **別イベントへの reversal_lane 適用**——統一パイプラインの 6 イベントの一部に対し K=3 を組む（gate から）。

優先度はつけない。director の手番。

---

## 5. 不変条件（崩さない）

- gate と real_d を混ぜない（gate=型/admissibility、real_d=測定）。
- K=2 で real_d を読まない（K_EXPECT=3）。
- engine 同定は名前でなく sha256（canonical `5d0239795333…`）。
- ABSTAIN / BLIND は honest output。FORWARD を成果として急がない。
- s 読出は readout であって発見ではない（割れていれば特にそう）。
- B13 は instrument。地震予測はしない。

---

## 6. code 状態

v167 zip のまま変更なし。本日 code の追加・編集なし。次回も v167 バンドルを使用。
engine 差し替え時は sha256 で canonical / stale を必ず確認（本日も冒頭で確認済）。

**END v169**
