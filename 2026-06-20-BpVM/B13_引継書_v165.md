# B13 引継書 v165（A-prep・実観測入口配線・prior 刻印・実データ待ち）

（v164 から継ぐ。v164 は B-4 で B-3 の内的防御軸が decoy_s 変動のみと確定し、防御の load-bearing 仮定を
「自然変動 prior」一点に isolate、A 解禁条件を満たした。
本セッションは director 固定の A-prep：v164 仕様をそのまま実観測 bundle の入口にし、prior を必ず刻む harness を組み、
玩具で *配線検査のみ* 通した。**実 real_d の成功は書いていない。実データ投入は director の手番。**）

---

## 0. このセッションの一行

A-prep 入口 harness 実装・配線 self-test 通過（4 経路）。**prior 宣言を毎回刻印。**
実観測 bundle の入口は `run_bundle(signals, target)` 一点。**実 real_d は捏造せず未投入のまま。**

---

## 1. 実装 〔v164 仕様そのまま・新防御なし〕

`a_prep.py`:
- 入口 `run_bundle(signals, target, n_surr)`：同一 target の実観測 bundle（まず K=3）を受ける。
- 各本 `single_member`：`check_timeseries`（条件1=raw 波形）を門前強制 → single_verdict / detected_s / real_d / guard_reason。
- BUNDLE 規則（v164）：**全本 FORWARD かつ detected_s 一致 → BUNDLE_FORWARD(s)／それ以外 → BUNDLE_ABSTAIN。**
- **prior 宣言を必ずログに刻む**（director 固定文・下記）。
- 診断のみ：全本 BLIND は「無反転」を ABSTAIN と区別して注記（headline は変えない）。

固定 prior 宣言文（毎回刻印）:
> BUNDLE_FORWARD は真の保証ではない。これは cross-signal stable dominant s の観測であり、
> 偽 decoy が自然観測条件で s を保つ場合は通過し得る。B-4 により、この防御は decoy_s 自然変動 prior に依存する。

---

## 2. 配線 self-test 〔玩具・配線検査のみ・実結果ではない〕

| 玩具 bundle | 期待配線 | 実際 | 一致 |
|---|---|---|---|
| 真支配（decoy 弱遠・s 振る） | BUNDLE_FORWARD(s=1) | BUNDLE_FORWARD(s=1) | ✓ |
| decoy 振る（支配・s 振る） | BUNDLE_ABSTAIN | BUNDLE_ABSTAIN（s 割れ） | ✓ |
| 反転なし（steady） | ABSTAIN + diag | BUNDLE_ABSTAIN + 全 BLIND diag | ✓ |
| 不変 decoy 支配 | BUNDLE_FORWARD(s=5 誤) | BUNDLE_FORWARD(s=5) | ✓（prior 境界・設計どおり） |

- prior 宣言は 4 経路すべてで刻印された。各本の guard_reason も出力（real-collapse / multibin split / clean）。
- **これは配線検査のみ。BUNDLE_FORWARD が出ても玩具信号で、実機 real_d の成功ではない。**

---

## 3. A 本体の手番 〔director〕

- 実観測の信号束を `run_bundle([obs0, obs1, obs2], target="...")` に差すだけ。harness が即 BUNDLE verdict を吐き prior を刻む。
- 入口で `check_timeseries` が非波形（residue 列・特徴量）を弾く（engine contract 条件1）。
- **実 real_d は実データが入って初めて生まれる。それまで成功は書かない。**

---

## 4. 残・要 director 判断（A と並行で可・急がない）

1. **BLIND の headline 扱い**：A-entry は二値 {FORWARD, ABSTAIN}。全本 BLIND（無反転）は現状 ABSTAIN に畳み diag で区別。
   これを独立 headline `BUNDLE_BLIND` にするか、二値のままにするか——情報は diag に保持済みなので軽微。要一言。
2. **保守性**（v163 §5-1）：unanimous-all-FORWARD の偽 ABSTAIN コスト。後から閾値調整可。
3. **より強い防御**：位相増分を変える変換（リサンプリング等）は B-4 で未検査・新防御になる。要るなら B-5 で*作る*（明示設計）。

---

## 5. 維持した規律

- **実 real_d を捏造しない。** A-prep は配線まで。実データ無しに成功を書かない、を貫いた。
- **prior を必ず刻印。** BUNDLE_FORWARD が真でないことを毎ログに残す配線にした。
- **新防御を作らない。** v164 仕様をそのまま入口にし、A-prep の役割をそこで止めた。
- **無反転と s-曖昧を混ぜない。** 全 BLIND を diag で分離（headline は仕様どおり二値）。

---

### 状態一覧（v164 から更新分）

| 項目 | 状態 |
|---|---|
| A-prep 入口 harness | **実装・配線 self-test 通過**（`a_prep.py`・4 経路） |
| prior 刻印 | **毎ログ強制・確定** |
| 実観測 plug | `run_bundle(signals, target)` 一点・条件1 門前強制 |
| 実 real_d | **未投入（捏造せず）**・director の手番 |
| BLIND headline 扱い | **要一言**（現状 ABSTAIN+diag） |
| A 本体 | **入口準備完了・実データ待ち** |

### 成果物（本セッション）

- `a_prep.py`（実観測 bundle 入口・v164 規則・prior 刻印・配線 self-test）
- 本引継書 v165（A-prep 配線完了と実データ待ちを畳んだ）

### これまでの格上げ履歴〔不可逆・確定〕

uncontested dominant（v162）→ cross-signal stable dominant（v163）→ 防御軸 decoy_s 単一・自然変動 prior 一点
に isolate（v164）→ 実観測入口に配線・prior 刻印（v165）。**真の保証はなお単一+束 harness の外。**

### 付録：検算済み群論事実 〔不変・再導出不要〕

- `diag(5,8)`：det≡1・位数4・平方=−I・∈SL₂(13)。`8I`：det≡−1・∉SL₂・中心スカラー。
- `U=[[1,1],[0,1]]`：位数13。`t U t⁻¹=U⁻¹`（split は couple）／`8I` は U と可換（doorway は decouple）。
- `⟨U,t⟩`=Dic₁₃(52)／`⟨U,8I⟩`=Z/52。det=1/−1 と couple/decouple は同じ一本の分岐。
