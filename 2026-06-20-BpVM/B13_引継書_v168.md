# B13 引継書 v168

**前版**: v167（code バンドル）
**日付**: 2026-06-26
**code**: 変更なし。**v167 zip をそのまま継続使用**（sha256 全一致・canonical `engine_reversal_eval.py`=5d0239795333 確認済）。本版は文書のみ。

---

## 0. 今日の到達点（一行）

旧来の Moran's I 系譜を「同一パイプライン6イベント」で引き直して決着をつけ、ハーネス（reversal_lane）は **K=3 gate 完成・real_d 直前**で規律通り停止した。

---

## 1. Moran's I 系譜（手順1）— 決着した

### 1.1 やったこと
6月25日 岩手県沖 M6.9（速報・深さ50km、722観測点）＋記事3の5イベント、**全6イベントを同一パイプラインで帯域別に引き直した**。
固定条件: haversine・150km二値・permutation N=999・seed=42・z場入力・`--band`出力。

### 1.2 確定（言える）
- 帯域分離 **lo ≪ mid ≈ hi は全6イベントで不変**。lo は常に最弱、mid/hi は有意な正。シリーズで最も再現性の高い観測。
- 6月25日はこの一般パターンに乗る（all +0.050、lo 最弱 +0.015、mid/hi +0.09〜0.10、near>far・逆転なし）。
- 余震①（本震+2日）のみ all/lo/mid 消失・hi のみ残存。

### 1.3 観測（条件付き）
- 同一パイプラインでは **2026系列の lo も有意**（記事3の ns とは逆）。
- 記事1（0.0737）・記事3（0.197）公表値は**本稿固定条件では完全には一致しない**。2026系列 all は一致、2011系列 all は系統差、の非対称。

### 1.4 言えないこと（フェンス）
- 「2026系列が低周波コヒーレンスを欠く」（記事3核心）→ 統一解析では**支持されなかった**。
- 「6月25日は B13的本震か否か」→ lo 有意性が処理条件依存ゆえ**二値判定しない**。
- 「記事3が誤り／統一が正しい」→ 元コード未参照。「固定条件では一致しない」事実のみ。
- 6月25日の余震・危険度→ instrument に留め**予測しない**。

### 1.5 §7 帰結
公表値（0.0737・0.197）は `確定`→`観測（元パイプライン依存・統一条件では未一致）` に降格。

### 1.6 成果物（outputs）
- `B13_その5_6イベント統一パイプライン_draft.md` … Chappy 査読反映済（「撤回」→「支持されなかった」等7箇所）。Zenn 入稿可（図は URL 差替）。
- `lo_reproduction.png` / `iwate_20260625_bands.png` / `iwate_20260625_zfield.png`

---

## 2. ハーネス（reversal_lane / 手順2）— K=3 gate 完成・real_d 直前で停止

### 2.1 機械健全性: PASS
a_prep 4経路セルフテスト全経路期待通り（真支配→FORWARD(1)／decoy振る→ABSTAIN／反転なし→ABSTAIN+diag／不変decoy→FORWARD(5誤・prior境界)）。機械は自分の限界に正直。

### 2.2 K=3 gate-only: 全点 admissible ✓
target `20260625073000`、real_d は未測定。

| station | net | 成分 | 震央/震源距離 | 長さ | 条1 | 条3,4 |
|---|---|---|---|---|---|---|
| MYG001 | K-NET | NS地表 | 157/165km | 164s | PASS | PASS (s読出4) |
| IWTH03 | KiK-net | ch4地表NS | 71/87km | 221s | PASS | PASS (s読出2) |
| IWT003 | K-NET | NS地表 | 41/65km | 185s | PASS | PASS (s読出6) |

- 成分規律: IWTH03 地表=ch4（振幅同定、ch1–3地中は除外）。K-NET 2点は地表NS単一。全点 demean 後。
- s読出（4/2/6）は**読み出しのみ・発見ではない**。verdict/real_d/gap99 未測定。
- IWT019（IWTH03 同位置 K-NET）は「同地点検証候補」別枠保存、bundle 不参加。
- 詳細: `B13_v168_bundle_section.md`。

### 2.3 prior（固定・据え置き）
> コーダ典型 SNR では ABSTAIN/全BLIND（real-collapse）が既定。FORWARD はまず prior 偽通過を疑う。

---

## 3. 次セッションの最短手順

1. prior 再掲（上記 2.3）。
2. **real_d 測定** = director の手番:
   `run_bundle([MYG001_NS, IWTH03_ch4, IWT003_NS], target="20260625073000")`
   （3点の生波形は `20260625073000_.zip`。MYG001/IWT003=knet col2、IWTH03=kik col5、各 demean）
3. 出力解釈: FORWARD でも prior 偽通過を疑う／ABSTAIN・BLIND は honest output として記録。
4. 結果を v169 に記録（real_d/gap99/verdict/bundle）。

並行可能（任意）:
- その5ドラフトの Zenn 入稿。
- 記事1/2系（2005・2022含む）も統一パイプラインへ（全系列を単一土台に）。

---

## 4. 不変条件（崩さない）
- gate と real_d を混ぜない（gate=型/admissibility、real_d=測定）。
- K=2 で real_d を読まない（K_EXPECT=3）。← 本日 K=3 達成。
- engine 同定は名前でなく sha256（canonical 5d0239795333）。
- ABSTAIN は honest output。FORWARD を成果として急がない。
- B13 は instrument。地震予測はしない。

---

## 5. code 状態
v167 zip のまま変更なし。本日 code の追加・編集なし。次回も v167 バンドルを使用。engine 差し替え時は sha256 で canonical/stale を必ず確認。

**END v168**
