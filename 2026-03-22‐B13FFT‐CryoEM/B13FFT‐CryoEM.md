---
title: "B13-FFT × クライオEM 実データ解析：アポフェリチン殻構造の選択的抽出"
emoji: "🔬"
type: "tech"
topics: ["cryoem", "fft", "python", "imageprocessing", "mathematics"]
published: false
---

> **コード・実行手順 → [GitHub リポジトリ]()** ← ここにGitHub URLを貼る

# B13-FFT × クライオEM 実データ解析

**B13格子フィルタによるアポフェリチン殻構造の選択的抽出**

バージョン: v63 (2026-03-22)

---

## 概要

B13-FFT は整数論的構造 `BASE = 3120 = 2⁴ × 3 × 5 × 13` に基づくフーリエビン選択フィルタである。通常の低域/高域フィルタとは異なり、B13格子に対応する特定周波数ビンのみを元画像から差し引いた**残渣**を出力する。

アポフェリチン（外径 ~120 Å、殻厚 ~20 Å）に対してスケール 119 Å を設定すると：

- 殻構造シグナルが残渣に選択的に保持される
- 背景ノイズおよびCTFリング成分が除去される
- 動径プロファイルのゼロ交差が殻壁位置（40 Å / 60 Å）に収束する

本稿は実クライオEMデータ（EMPIAR-10146相当、1.34 Å/px）を用いた v63 の理論・実験結果・証明を記録する。

---

## 1. 理論背景

### 1.1 B13ビンの定義

B13格子点の方位角インデックスは以下で与えられる：

```
az_idx = ((5k + 12j) mod 60) × 52  mod 3120
    k ∈ {0, 1, ..., 11},  j ∈ {0, 1, ..., 4}
```

物理スケール $s$ [Å] とピクセルサイズ $d$ [Å/px] から `scale_factor = round(s / d)` を計算し、ビンをピクセル空間に写像する。アポフェリチン解析では $s = 119$ Å、$d = 1.34$ Å/px として `scale_factor = 89`。1240 px 行に対して **100ビン（全体の 8.1%）** が選択される。

### 1.2 残渣の計算

行・列を独立に処理し、その平均を残渣とする：

```
residual_row = image − IFFT(FFT_row  ×  B13_mask_row)
residual_col = image − IFFT(FFT_col  ×  B13_mask_col)
residual_2d  = (residual_row + residual_col) / 2
edge_product = |residual_row| × |residual_col|
```

`edge_product` は円形構造の境界を強調し、粒子検出の根拠となる。行と列を別々に処理する点が通常の2D-FFTフィルタとの根本的な違いである。

### 1.3 殻検出スコアの定義（v63 新規）

各粒子の残渣プロファイルから以下を計算する：

$$\text{score} = \text{trough} \times (1 + \text{sharpness} \times 50)$$

| 成分 | 定義 |
|------|------|
| trough | $r = 20$〜$38$ Å の残渣プロファイル最小値の絶対値 |
| sharpness | $r = 35$〜$48$ Å における最初のゼロ交差前後の差 |

係数 50 は trough と sharpness のスケールを揃えるための経験的正規化係数。

---

## 2. Fib/Pell の 4H 回避定理（v63 完全証明）

$\mathbb{Z}_{13}^*$ の部分群を $H = \{1, 5, 8, 12\}$、そのコセットを $2H = \{2, 3, 10, 11\}$、$4H = \{4, 6, 7, 9\}$ と定義する。

**定理:** すべての $n \geq 0$ に対して $F(n) \bmod 13 \notin 4H$、および $P(n) \bmod 13 \notin 4H$

**証明（3ステップ）:**

**Step 1** — 生成行列のスカラー冪（直接計算）

$$M^7 \equiv 8I \pmod{13}, \quad 8 \in H$$
$$N^7 \equiv 5I \pmod{13}, \quad 5 \in H$$

**Step 2** — $H$ の乗法閉包（数値確認済み）

$$H \times (H \cup 2H) = H \cup 2H \quad (4H \text{ を含まない})$$

**Step 3** — 帰納的完結

$n = 7q + r$ と書くと：

$$[F(n+1), F(n)]^T = M^n \cdot [1,0]^T = 8^q \cdot (M^r \cdot [1,0]^T)$$

$8^q \in H$（Step 1）、$M^r \cdot [1,0]^T \in \{0\} \cup H \cup 2H$（$r = 0, \ldots, 6$ の直接確認）、$H \times (\{0\} \cup H \cup 2H) = \{0\} \cup H \cup 2H$（Step 2）

$$\therefore \quad F(n) \bmod 13 \notin 4H \qquad \square$$

**証明状態: ✓ 完全（代数的に閉じた証明、v63確定）**

---

## 3. 実験結果（v63）

### 3.1 解析パラメータ

| パラメータ | 値 |
|-----------|-----|
| 入力データ | May08_03.05.02.bin.mrc / May08_07.30.46.bin.mrc |
| 画像サイズ | 1200 × 1240 px |
| ピクセルサイズ | 1.34 Å/px |
| scale_factor | 89 = round(119 / 1.34) |
| outer_r / inner_r | 60 Å / 40 Å |
| フレーム数 | 各50（全フレーム平均） |

### 3.2 定量結果

| 指標 | 値 |
|------|-----|
| 解析粒子数 | 91 / 101（端部除外後） |
| 殻スコア mean ± std | 0.204 ± 0.121 |
| 殻スコア最大値 | 0.585（cy=939, cx=497） |
| 動径ゼロ交差（内壁） | 41 Å（設計値 40 Å、誤差 1 Å） |
| 動径ゼロ交差（外壁） | 58 Å（設計値 60 Å、誤差 2 Å） |
| 対称性ミスマッチ S4/S5 | 1.325（> 1 → O群優勢） |

### 3.3 可視化結果

![B13-FFT解析結果（v63）](https://raw.githubusercontent.com/[USER]/[REPO]/main/b13_result.png)
↑ **ここに GitHub の画像 raw URL を貼る**（例: `https://raw.githubusercontent.com/yourname/repo/main/b13_result.png`）

| パネル | 内容 |
|--------|------|
| 左上 | 生画像平均。シアン円=外壁60Å、黄色=内壁40Å。ライムグリーン=殻スコアTOP25%（23粒子） |
| 中上 | B13-FFT残渣2Dマップ（RdBu_r）。粒子位置で同心円パターンが明確 |
| 右上 | エッジ積 \|row_res\|×\|col_res\|。粒子境界が輝点として現れる |
| 左下 | 生画像の動径プロファイル（91粒子平均）。バックグラウンド勾配が支配的 |
| 中下 | B13残渣の動径プロファイル。41/49/58/66/74 Åのゼロ交差（緑線）が殻構造を反映 |
| 右下 | 殻スコア上位6粒子の残渣パッチ。全粒子で同心円パターンが再現 |

---

## 4. CTFリング仮説の検証（v63 棄却）

### 仮説

「B13-FFTが選択的に除去するのは殻シグナルではなく、CTF（Contrast Transfer Function）リング構造ではないか」

### 結果

残渣パワーピークとアポフェリチン既知構造との対応：

| パワーピーク | 対応構造 | CTF零点との対応 |
|-------------|---------|----------------|
| 54〜55 Å（最大） | 殻中央半径 (60+40)/2 ≈ 50 Å | なし |
| 60 Å | 外壁半径（完全一致） | なし |
| 115 Å | B13スケール 119 Å（差 4 Å） | なし |

### 結論

残渣パワーピークはアポフェリチンの既知構造寸法と系統的に整合し、CTF零点とは対応しない。**CTFリング仮説は棄却。** B13-FFTは背景ノイズを優先的に除去し、殻の実構造シグナルを保持している。上位6粒子パッチ（Panel 6）でも全粒子に同心円パターンが再現されており、これがCTFリング仮説棄却の視覚的根拠でもある。

---

## 5. 対称性ミスマッチの定量（S4/S5 = 1.325）

### 背景

B13格子はIh群（正二十面体、5回対称）を前提に設計されているが、アポフェリチンはO群（432点群、4回対称）を持つ。この対称性ミスマッチをB13残渣の角度方向FFTで定量した。

### 測定結果（91粒子）

| 対称成分 | 平均パワー比 ± std | 意味 |
|---------|-----------------|------|
| 4回対称（O群特有） | 0.1161 ± 0.0992 | アポフェリチンの4回軸 |
| 5回対称（Ih群特有） | 0.0876 ± 0.0777 | B13の設計基底 |
| 6回対称（参照） | 0.0896 ± 0.0813 | — |
| **S4/S5** | **1.325** | **> 1 → O群優勢** |

### 解釈

S4/S5 = 1.325 > 1 はB13残渣がO群の4回対称性を保持していることの定量的証拠である。Ih群の5回成分（B13の設計基底）は実データに有意に現れておらず、これが対称性ミスマッチの定量的証拠となる。このミスマッチは除去できないが、**バグではなく試料と格子の対称性の不一致**であり、残渣に4回対称パターンが残る原因として理解される。

---

## 6. B13パッケージ基本定数

| 定数 | 値 |
|------|-----|
| BASE | 3120 = 2⁴ × 3 × 5 × 13 |
| scale_factor（119 Å） | 89 = round(119 / 1.34) |
| B13ビン数（1240 px） | 100ビン = 8.1% |
| $H$ | $\{1, 5, 8, 12\}$（$\mathbb{Z}_{13}^*$ の部分群） |
| $2H$ | $\{2, 3, 10, 11\}$ |
| $4H$ | $\{4, 6, 7, 9\}$（Fib/Pellの禁止域） |

---

## 一行結論

**B13残渣はCTFではなくアポフェリチン殻構造シグナルを選択的に抽出し、O群4回対称性（S4/S5 = 1.325）を保持している。Fib/Pellの4H回避は $M^7 \equiv 8I \pmod{13}$ で代数的に証明された。**

---

> **コード・実行手順 → [GitHub リポジトリ]()** ← ここにGitHub URLを貼る
